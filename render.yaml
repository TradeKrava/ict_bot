services:
  - type: worker
    name: ict-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python ict_bot.py
    autoDeploy: true
    envVars:
      - key: TG_TOKEN
        sync: false
